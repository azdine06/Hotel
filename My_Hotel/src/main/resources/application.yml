
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/My-hotel
    username: Hotel
    password: passe
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
